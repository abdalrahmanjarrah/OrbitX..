# 09 — لوحة التحكم والتصنيفات والإحصائيات

> **المجلد:** `OrbitX-source/src/views/` + `OrbitX-source/src/components/`
> **الملفات:** Dashboard.tsx، HomeView.tsx، LeaderboardView.tsx، FocusHeatmap.tsx، AnalyticsView.tsx، BlackHolesView.tsx، UserSearchView.tsx
> **الحجم الإجمالي:** 3020 سطراً تقريباً (909 + 759 + 310 + 229 + 329 + 361 + 123)

---

## 1. وظيفة القسم — يعني ×3

- **يعني...** هذا القسم هو "واجهة المنصة" كلها: الصفحة الرئيسية التي تشوفها أول ما تدخل، وقائمة المتصدرين، وعدّاد إنجاز المجرة الجماعي، والبحث عن الأصدقاء.

- **يعني...** تخيّل تطبيقاً يحول الدراسة إلى لعبة فضاء. لوحة التحكم هي «الطيار الآلي» الذي يوجّهك بين المحطات: سهم للتركيز، سهم للمجرة، سهم لهويتك. والتصنيفات هي «لوحة النتائج» التي تريك مين صاحب أعلى نقاط XP، والثقوب السوداء هي «تحدي جماعي» يجمع ساعات الجميع لفتح جائزة سرية.

- **يعني...** تقنياً: `Dashboard.tsx` هو الهيكل الحاوي (Shell) الذي يختار أي مكوّن يُعرض حسب التبويب النشط ويحمّل الصفحات كسولاً (Lazy) لتسريع الإقلاع. `HomeView.tsx` يجلب الغرف النشطة من مجموعة `rooms` في Firestore. `LeaderboardView.tsx` يقرأ `profiles` مرتبة حسب `xp` تنازلياً. `BlackHolesView.tsx` يجمّع XP لأعلى 100 مستخدم ويحوّله لساعات. أما `FocusHeatmap` و`AnalyticsView` فهما إحصائيات، لكن انتبه: **بياناتهما مزيّفة بالكامل (عشوائية)** ولا تُقرآن من قاعدة البيانات إطلاقاً — التفاصيل في القسمين 6 و7.

---

## 2. الملفات المشمولة

| الملف | السطور | الدور |
|---|---|---|
| `src/views/Dashboard.tsx` | 909 | الحاوية الرئيسية: تبويبات، شريط علوي، Dock سفلي، معالج Onboarding، تحويل بين الصفحات |
| `src/views/HomeView.tsx` | 759 | الصفحة الرئيسية: المحطات النشطة، إنشاء/الانضمام بالرمز، إحصائيات سريعة |
| `src/views/LeaderboardView.tsx` | 310 | قائمة أفضل 50 رائداً حسب XP |
| `src/views/FocusHeatmap.tsx` | 229 | خريطة حرارية لـ 60 يوماً — **بيانات عشوائية** |
| `src/views/AnalyticsView.tsx` | 329 | رسوم بيانية (خطي/عمودي) — **بيانات عشوائية وغير مستخدمة** |
| `src/views/BlackHolesView.tsx` | 361 | التحدي الجماعي: عدّاد الساعات، أفضل 3 مساهمين |
| `src/components/UserSearchView.tsx` | 123 | بحث عن المستخدمين بالاسم |

---

## 3. لوحة التحكم (Dashboard.tsx)

### ما تعرضه
- **شريط علوي عائم** (L618-780): شعار OrbitX الدوّار، أزرار الفئات، مبدّل اللغة، زر الجولة الإرشادية، الإشعارات، دعوة صديق، وتغليف هوية المستخدم.
- **تغليف الهوية/الإحصائيات** (L749-778): الصورة، الاسم، رتبة `getAstronautRank(user.xp)` (L755-758)، ونقاط XP (L763-765).
- **معالج Onboarding** من 3 خطوات (L222-224 للدولة، وL389-606 للواجهة): خطوة 0 ترحيب + هوية (L416-457)، خطوة 1 اختيار التخصص/الوظيفة (L460-531)، خطوة 2 تحديد الهدف اليومي ثم حفظ `missionRole` (L534-602).
- **بانر وضع المشاهدة** للضيف مع زر تسجيل (L783-806).
- **القائمة السفلية العائمة Dock** (L877-906): ثلاث فئات (تركيز / مجرة / هوية) عبر `DockButton`.
- **شريط تبويبات مساعد للموبايل** (L845-874).
- **تبديل المحتوى** (L809-842): كل تبويب يحمّل صفحته كسولاً (Suspense + Lazy).

### تحويل الصفحات (Lazy Imports) — L153-165
يحمّل لاحقاً: HomeView, StudyRoomView, LeaderboardView, ProfileView, DiscussionsView, ScheduleView, AdminView, SupportView, BlackHolesView, AwarenessView, ChallengesHubView, FleetsView, UserSearchView.

### المعالجة الرئيسية — L280-298
إذا كان المستخدم داخل محطة نشطة (`activeStation`) تُعرض `StudyRoomView` فوراً بدل لوحة التحكم.

### المستمعون والدوال الرئيسية
| الدالة/المستمع | السطر | الوظيفة |
|---|---|---|
| `onSnapshot(...)` (غلاف ملتقط للأخطاء) | 120-137 | يلتف حول `onSnapshot` الأصلي لاعتراض أخطاء الاشتراك |
| Effect البحث عن محطة نشطة | 198-219 | `getDocs` على `rooms` حيث `participants array-contains user.uid` لاستعادة الجلسة تلقائياً |
| Effect الـ scroll | 240-246 | يفعل/يعطّل خلفية الشريط العلوي |
| `handleJoyrideCallback` | 251-257 | إنهاء الجولة الإرشادية وحفظ `hasSeenTour_v3` |
| `handleSelectRole` | 261-273 | `updateDoc` على `users/{uid}` لكتابة `missionRole` |
| `handleSkipRole` | 275-278 | حفظ `hasSkippedRoleModal` |
| `setCategory` | 312-316 | تحويل فئة الـ Dock إلى تبويب افتراضي |
| `handleInviteCopy` | 318-330 | نسخ رابط دعوة عبر `buildInviteLink` |
| `handleTabChange` | 332-357 | يحدد التبويب، يختار نص نشاط، وبعد **4 ثوان** (debounce) يكتب `currentActivity` إلى `users/{uid}` |
| حالة `showRoleModal` | 222-224 | يُفتح تلقائياً لمن لا دور له ولم يتخطَّ |

### ملاحظات
- الأزرار المتعلقة بالدور الإداري (admin) معتمدة على `user.role` الواردة من الواجهة فقط (L665-667, L868-870).
- الضيف مقيد بتبويبات: home, discussions, leaderboard, blackholes (L179, L300-306, L333-336).

---

## 4. الصفحة الرئيسية (HomeView.tsx)

### المحتوى
- **ترحيب مخصص بالوقت** `getGreetingForTime` (L218-220) + اسم المستخدم (L437).
- **زر إنشاء محطة** للمسجلين + حقل رمز انضمام (L444-485).
- **بطاقتا إحصائيات**: ساعات التركيز = `xp / 60` (L494)، والمستوى والرتبة (L502).
- **لوحة النزال الأسبوعي** `WeeklyDuelPanel` (L508).
- **قائمة المحطات** مع بحث + فلاتر (الكل/محطاتي/عامة/خاصة) + تبديل شبكة/قائمة (L526-576)، العرض عبر `StationCard` شبكة أو قائمة (L596-621).
- **نافذة إنشاء محطة** (L627-755): اسم، خاص/عام، خلفية من 6 صور جاهزة (L304-311).

### جلب البيانات — الدالة `fetchData` (L222-302)
| المصدر | الاستعلام | الحد |
|---|---|---|
| الغرف `rooms` | `orderBy createdAt desc` | `limit(50)` (L231) |
| النصيحة `advices` | `orderBy timestamp desc` | `limit(1)` (L264) |
| المستخدمون النشطون `profiles` | `orderBy lastActiveTime desc` | `limit(15)` (L271) |

تفاصيل مهمة داخل `fetchData`:
- **استبعاد التحديات** من الغرف: `data.isChallenge` يُتجاهل (L241-243).
- **الغرف الخاصة**: تُخفى إلا لمن في `participants` (L245-247).
- **حذف الغرف الفارغة من جهة العميل**: إذا `participants.length === 0` وكانت فارغة أكثر من **5 دقائق** (`emptyAt` + 300000ms) ينفذ `deleteDoc` (L248-256). ⚠️ أي مستخدم يفتح الصفحة هو من ينفّذ الحذف.
- **تحديث المستخدمين النشطين** كل **3 دقائق** عبر `setInterval(fetchActiveUsers, 180000)` لتقليل قراءات Firestore (L288-289)، مع فشل لين (Soft Fail) (L283).

### الدوال الرئيسية
| الدالة | السطر | الوظيفة |
|---|---|---|
| `generateJoinCode` | 313-320 | كود مكوّن من 6 رموز دون أحرف ملتبسة (بدون I/O/1/0) |
| `handleJoinByCode` | 322-353 | يبحث عن غرفة بـ `joinCode` (L331-335)، يضيف المستخدم عبر `arrayUnion`، ثم يدخل |
| `handleCreateRoom` | 355-402 | `addDoc` في `rooms` ببيانات افتراضية (5 مشاركين كحد أقصى، 25 دقيقة/5 راحة) |
| `filteredRooms` (useMemo) | 404-419 | فلترة حسب الفلتر/البحث، ثم فرز: المحطات في وضع `focus` أولاً ثم الأكثر مشاركةً |

---

## 5. التصنيف (LeaderboardView.tsx)

### كيف يُجلب
دالة `fetchLeaders` (L174-197): استعلام على `profiles` بـ `orderBy("xp", "desc")` مع `limit(50)`، ثم فلترة `u.displayName` الموجود. **يُجلب مرة واحدة عند التركيب** والاعتمادات `[]` (L197) — لا تحديث تلقائي، ولا اشتراك لحظي.

### الترتيب
يُعرض بالترتيب الفعلي للاستعلام. أول 3 مراكز لها ألوان خاصة (ذهبي/فضي/برونزي) (L226-233)، وصف المستخدم الحالي مميز بخلفية `bg-violet/10` (L243) ونجمة على صورته (L267-271).

### الفلترة
**لا توجد أي فلترة أو بحث أو ترقيم صفحات (Pagination)** — كل ما في الأمر أول 50 حسب XP. عنوان البطاقة "أفضل 50 رائد فضاء" (L211-213) يطابق الواقع، لكن لا توجد خيارات تصفية أخرى رغم تسمية القسم.

### التفاعل
النقر على أي مستخدم يستدعي `onSelectUser(uid)` لفتح `UserModal` (L258-276).

---

## 6. الخريطة الحرارية (FocusHeatmap.tsx)

### ماذا تعرض
شبكة من 60 مربعاً تمثل آخر 60 يوماً (L181-185)، لون كل مربع حسب عدد دقائق التركيز عبر `getDayColor` (L187-194): صفر = داكن، ثم وردي خفيف (أقل من 30د)، وردي (أقل من 60د)، بنفسجي (أقل من 120د)، وبنفسجي فاتح نابض (120د+). مع أسطورة ألوان (L218-226) وعنوان "نشاط المجرة (التركيز)" (L198-201).

### من أين تأتي البيانات ⚠️
**لا مصدر إطلاقاً — البيانات عشوائية.** في `useMemo` (L164-177):
```js
if (Math.random() > (isRecent ? 0.3 : 0.6)) {
  hist[dateStr] = Math.floor(Math.random() * 150);
}
```
- الأيام الأقرب (أقل من 15 يوماً) عندها احتمال 30% أن تبقى فارغة، والأبعد 60% فارغة، والبقية قيم عشوائية بين 0-149 دقيقة.
- لا يوجد أي استدعاء لـ Firestore، ولا يمر أي بروب (Props) للواجهة.
- النتيجة: مكوّن "إحصائية" زينة فقط، وبياناته غير حقيقية وتُعرض وكأنها فعلية.

---

## 7. التحليلات (AnalyticsView.tsx)

### الرسوم البيانية
- **رسم خطي** "ساعات التركيز" لآخر 7 أيام (L230-266) عبر `recharts` (LineChart).
- **رسم عمودي أفقي** "تصنيفك بين الأصدقاء" (L277-322): يقارن `friends` + المستخدم الحالي، يُرتب حسب `level*100 + xp`، يأخذ أول 5، ويسمي المستخدم الحالي "أنت" (L201-210).
- عدّاد "مجموع جلسات التركيز: `user.totalFocusSessions`" (L219).

### المصادر ⚠️
- بيانات الخط (L183-199): **وهمية** — ساعات مبنية على `(user.xp % 10) / 2` + قيمة عشوائية `Math.random() * 2 - 1`، لا تُقرأ من قاعدة البيانات.
- بيانات المقارنة بين الأصدقاء (L201-210): حقيقية (من بروب `friends`)، لكن `friends` لم يُمرر إليها في أي مكان.

### حالة الاستخدام ⚠️
`AnalyticsView` **غير مستوردة في أي ملف آخر** (بحث شامل أكد أن السطر 175 وحده هو تعريفها). أي: مكوّن منسي لا يظهر للمستخدمين إطلاقاً، ومع ذلك يحمل بيانات إحصائية مزيفة.

---

## 8. الثقوب السوداء (BlackHolesView.tsx)

### ما يعرضه
- تحدٍ جماعي هدفه **1000 ساعة** تركيز مجمعة (`targetGoal = 1000`, L168).
- **عدّاد التقدم** الدائري وشريط التقدم (L252-287) مع نسبة `progressPercent` المقصوصة عند 100 (L205).
- **الجائزة المخبأة** "ملف مشفر" (L291-313).
- **أفضل 3 مساهمين** (L315-355) مع ساعاتهم (`xp / 60`).
- **خلفية ثقب أسود متحركة** بـ `motion` (L228-249).

### كيف يُكتشف/يُحسب — `fetchProgress` (L171-203)
1. استعلام `profiles` بـ `orderBy("xp","desc")` و`limit(100)` (L174-178).
2. يجمع `xp` لكل مستخدم في متغير اسمه `totalSessions` (تعليق L184-185 يقر: "نعيد استخدام totalSessions لكنه في الواقع إجمالي XP").
3. `totalHours = totalSessions / 60` بافتراض **ساعة تركيز = 60 XP** (L195-197) → `globalProgress`.

### المشكلة المفاهيمية ⚠️
- "التحدي الجماعي" في الواقع **لا يجمع كل المستخدمين**: فقط أعلى 100 حسب XP (L177). أي مستخدم بعد المركز 100 لا يُحسب رغم صياغة "تعاونوا مع جميع الرواد".
- النص يقول "تجميع **1000 ساعة هذا الأسبوع**" (L268)، لكن الكود **لا يفلتر بأسبوع إطلاقاً** — يحسب كل XP المتراكم عبر الزمن كله.
- لا يوجد أي "كشف عن مستخدمين نشطين/وهميين" — المصطلح في التسمية لا يطابق التنفيذ؛ القائمة هي أعلى 3 في XP فقط (L190-193)، والعد يحسبهم جميعاً دون تمييز نشاط.

---

## 9. البحث عن المستخدمين (UserSearchView.tsx)

- **واجهة** (L45-121): عنوان "المحققون"، حقل بحث بعلامة تحميل دائرية أثناء الجلب (L68-72)، ونتائج كبطاقات تعرض الصورة، الاسم، الرتبة عبر `getAstronautRank` (L95)، والمستوى (L100-103). النقر على أي نتيجة يفتح ملف المستخدم `onSelectUser` (L85). حالة "لا نتائج" عند غياب المطابقات (L107-116).
- **الآلية** (L14-43): debounce **500ms** قبل إجراء البحث. شرط الحرفين كحد أدنى (L16-19).
- **الاستعلام** (L22-29): نطاق بادئة (Prefix Range) على `displayName`:
  - `where('displayName', '>=', term)` و`where('displayName', '<=', term + '\uf8ff')`
  - مع `limit(20)`.
- **ملاحظات**:
  - النتائج تُبنى بـ `doc.data()` فقط (L32) **دون إضافة `uid` من `doc.id`** — يعتمد على وجود حقل `uid` داخل المستند نفسه، خلافاً لـ `BlackHolesView` التي تضيفه يدوياً (L186). تناقض في المعالجة.
  - لا Pagination: أي نتيجة بعد العشرين غير ظاهرة حتى لو وُجدت مطابقات.
  - لا استبعاد للمستخدم الحالي من النتائج.

---

## 10. نقاط القوة

1. **هندسة تقسيم الكود (Code Splitting) ممتازة** — كل الصفحات تُحمَّل كسولاً بـ `React.lazy` + `Suspense` مع fallback بصري متسق (Dashboard.tsx:153-165, 819-826)، وهذا يسرّع الإقلاع كثيراً.
2. **تصميم بصري موحّد ومتجانس** — نظام فضاء كوني متقن (خلفيات متدرجة، توهجات، حركات `motion`)، واتجاه RTL/LTR مدعوم ديناميكياً عبر `dir` وحزمة `useLanguage`.
3. **معالجة أخطاء Firestore موحدة** — كل الواجهات تفتح القراءة عبر غلاف `onSnapshot` (Dashboard.tsx:120-137) مع `handleFirestoreError` لتقليل الأعطال غير المحصورة.
4. **ترشيد جلب البيانات في HomeView** — تحديث "المستخدمين النشطين" كل 3 دقائق عبر Interval بدل اشتراك لحظي، مع فشل لين (HomeView.tsx:271-289).
5. **سلوك "استعادة الجلسة"** — إذا دخل المستخدم وهو داخل محطة، يعيد فتحها تلقائياً (Dashboard.tsx:198-219, 280-298).
6. **فرز ذكي للمحطات** — محطات التركيز النشطة أولاً ثم الأكثر مشاركةً (HomeView.tsx:413-418).
7. **وضع ضيف محسوب** — قائمة تبويبات مقيدة وواجهة مشاهدة واضحة مع دعوة للتسجيل (Dashboard.tsx:179, 300-306, 783-806).

---

## 11. نقاط ضعف وملاحظات للتعديل ⚠️

1. **الخريطة الحرارية «إحصائية وهمية»** — `FocusHeatmap.tsx:164-177`: البيانات كلها `Math.random()`، ومع ذلك تُعرض بعنوان "نشاط المجرة" وأسطر ألوان، ما يضلل المستخدم وكأنها نشاط حقيقي. إما يُربط ببيانات Firestore فعلية أو يُعلَّم بوضوح كأنه "معاينة/تجريبي".
2. **AnalyticsView مزيف وغير مستخدم** — `AnalyticsView.tsx:183-199` بيانات عشوائية، والمكوّن كله غير مستورد في أي مكان (السطر 175 وحده هو تعريفه). ملف منسي بحاجة لحذف أو ربط ببيانات حقيقية.
3. **عدّاد الثقب الأسود ليس جماعياً فعلاً** — `BlackHolesView.tsx:177`: `limit(100)` يقتصر على أعلى 100 مستخدم، و(196-197) يحسب كل XP تراكمي دون فلتر أسبوع رغم نص "هذا الأسبوع" (L268). الحل: استعلام بعدد الجلسات/الساعات داخل نافذة أسبوع حقيقي أو دالة مجمعة (Aggregation).
4. **حدود الـ limit بدون Pagination في كل مكان** — محطات 50 (HomeView:231)، نشطون 15 (HomeView:271)، تصنيف 50 (Leaderboard:181)، بحث 20 (UserSearchView:29)، تقدم جماعي 100 (BlackHoles:177). عند نمو المستخدمين ستُقتطع البيانات بصمت.
5. **التصنيف لا يتحدث أبداً** — `LeaderboardView.tsx:174-197` يجلب مرة واحدة بإعتماد `[]`؛ لا إعادة جلب عند العودة للتبويب ولا اشتراك لحظي. رتبة المتصدرين ستتجمّد.
6. **حذف الغرف الفارغة من جهة العميل** — `HomeView.tsx:248-256`: أي زائر يفتح الصفحة قد يحذف مستند `rooms` إذا كان فارغاً أكثر من 5 دقائق، دون قيود صلاحيات (قد يحذف غرفة أنشأها غيره). يجب نقله لوظيفة خادم (Cloud Function/onCall).
7. **كتابة `currentActivity` مع كل تنقل** — `Dashboard.tsx:351-356`: كل تغيير تبويب = `updateDoc` على `users/{uid}` بعد 4 ثوان. كثرة الكتابة قد ترفع التكلفة والقراءات.
8. **اعتماد الأمان على واجهة المستخدم** — تبويب الإدارة `user.role === "admin"` (Dashboard.tsx:665, 868) وكل القيود معتمدة على `user` الوارد من الواجهة؛ لا وجود لتحقق خادمي (Rules) مذكور هنا.
9. **نطاق البحث يفتقر `uid`** — `UserSearchView.tsx:32` يبني النتائج من `doc.data()` فقط؛ إن لم يحتوِ المستند على `uid` فستتعطل `key` والنقر على الملف. مقابل `BlackHolesView.tsx:186` التي تضيفه يدوياً — غير متناسق.
10. **نمط `onSnapshot` المكرر النسخ** — نفس الغلاف (سطر ~120 في كل ملف) منسوخ في ~22 ملفاً، وبعضه دون استخدام فعلي هنا (هذه الواجهات تعتمد `getDocs`). يُفضَّل استخراجه لملف واحد مشترك.
11. **واردات ميتة ضخمة** — HomeView يستورد Dashboard وLeaderboardView وFocusHeatmap وUserSearchView (L142,156,157,116) وغيرها دون استخدامها؛ وكل ملف يستورد ~100 أيقونة lucide معظمها غير مستخدم. يضخّم الحزمة ويشتت القارئ.

---

## 12. أشياء غريبة / كود مشبوه

1. **إحصائية عشوائية تُعرض كحقيقة** — `FocusHeatmap.tsx:199` عنوان "نشاط المجرة (التركيز)" فوق بيانات `Math.random()`؛ هي أكثر نقطة تضليل في القسم.
2. **اسم متغير مضلل مع تعليق يعترف بذلك** — `BlackHolesView.tsx:185`: "We reuse totalSessions but as total XP" — الكود يحسب XP داخل متغير اسمه جلسات.
3. **"هذا الأسبوع" بلا أسبوع** — `BlackHolesView.tsx:268` يعد المستخدم بـ "1000 ساعة هذا الأسبوع"، والتنفيذ يجمع كل التاريخ بلا أي فلتر زمني.
4. **FocusHeatmap مستوردة في ~20 ملفاً لكنها لا تُستخدم أبداً** — بحث `<FocusHeatmap` لم يجد أي استخدام فعلي؛ مستوردة في HomeView (157) وLeaderboardView (152) وBlackHolesView (153) وغيرها كلها إهدار.
5. **ترخيص مكرر مرتين** في رأس كل ملف تقريباً (مثل Dashboard.tsx:6-9 ثم 11-14، وHomeView.tsx:2-5 ثم 7-10).
6. **`HomeView` تعدّل قاعدة بيانات باسم الزائر** — الحذف التلقائي للغرف الفارغة (HomeView.tsx:253) ينفذه أي مستخدم عشوائي يفتح الصفحة، وهو سلوك جانبي غير متوقع.
7. **افتراض ثابت 60 XP = ساعة** محسوب بقسمة يدوية في مكانين (HomeView:494 وBlackHoles:196) دون تعريف مركزي — لو تغيّرت صيغة XP انكسرت كلتا الإحصائيتين.
8. **`UserSearchView` بلا حارس ضيف/دور** — القيد فقط من Dashboard عند إخفاء تبويب البحث عن الضيف (L652)؛ المكوّن نفسه لا يتحقق من أي شيء.

---

### ملاحظات ختامية
- المسار: `OrbitX-source/src/views/` (مع `components/UserSearchView.tsx`).
- أقوى القواعد في هذا القسم: التصميم الموحّد، تقسيم الكود، ومعالجة أخطاء Firestore.
- أهم ما يستحق المعالجة فوراً: الربط ببيانات حقيقية للخريطة الحرارية والتحليلات، إصلاح عدّاد الثقب الأسود (limit 100 + فلتر الأسبوع)، ونقل حذف الغرف الفارغة إلى جهة خادم آمنة.
